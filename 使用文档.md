# AI 驱动小说角色感知与叙事编排引擎使用文档 (Novels Engine User Manual)

> **版本**：v0.1.0 (Rust 2024 Edition)  
> **核心驱动**：DeepSeek V4 Flash (`deepseek::DEEPSEEK_V4_FLASH`) + 1.2 万条原著蒸馏语料库 + SQLite 0.8  
> **架构设计**：`novels::prompt` (提示词解耦) · `novels::scene::StoryService` (业务编排) · `novels::prose` (BM25 重排与抗 AI 腔拼装)

---

## 目录

1. [项目简介与防 AI 腔核心原理](#一-项目简介与防-ai-腔核心原理)
2. [环境配置与快速开始](#二-环境配置与快速开始)
3. [核心 CLI 命令与质量门禁](#三-核心-cli-命令与质量门禁)
4. [N5 长线物理状态机使用指南](#四-n5-长线物理状态机使用指南)
5. [提示词工程与确定性生成指南](#五-提示词工程与确定性生成指南)
6. [Web 工作台与 RESTful API 接口说明](#六-web-工作台与-restful-api-接口说明)

---

## 一、 项目简介与防 AI 腔核心原理

**Novels Engine** 是一个解决 AI 小说创作中“AI 因果说明文套话”、“描写注水堆砌”、“跨书词汇串味”及“长线逻辑割裂”等痛点的生产级叙事引擎。

### 五重防 AI 腔安全护栏：
1. **原著描写硬引用 (High Quote Density)**：正文关键描写严格从 12,471 条原著蒸馏语料中提取，保证 **原著引用密度 $\ge 30.0\%$**（实测达到 84.36%）。
2. **BM25 语义与声口重排**：结合 BM25 算法 (k1=1.2, b=0.75) 与角色专属声口加权（Voice Boost），实现精准贴戏。
3. **拼装节奏与书源隔离**：正文段落使用 `，`/`。` 平滑连接；单 Beat 内部强制保持主导书源（如 `hlm` 或 `zhz`），防止跨书串味。
4. **N3/N4 结构化代数槽位**：
   * **N3 结构化动作 (`StructuredAction`)**：动作限制为舞台剧提示格式（如 `LookAt`/`Pause`/`Exclaim`）。
   * **N4 结构化原因 (`PlotReasonSlot`)**：剧情原因收敛为 `Observed`、`BehaviorOdd` 等代数槽位，**0 产生“由于/因此/从而”等 AI 说明文**。
5. **N5 长线物理状态机 (`CharacterState`)**：引入物理/叙事状态控制（死亡、不在场、负伤、持物），拦截死亡或不在场角色的非法推导。

---

## 二、 环境配置与快速开始

### 1. 环境变量配置
在项目根目录创建 `.env` 文件（已默认添加到 `.gitignore`）：

```ini
# DeepSeek API Key (用于生产模型推理 rig::providers::deepseek)
# 若未提供，系统会自动平滑降级为 Mock 生成器，不会发生崩溃
DEEPSEEK_API_KEY="your-deepseek-api-key-here"

# (可选) 自定义蒸馏语料库路径
# NOVELS_DISTILLED_DIR="assets/distilled"

# (可选) 跳过蒸馏合并，仅加载基础词库
# NOVELS_SKIP_DISTILLED=1
```

### 2. 运行默认示范应用
```powershell
cargo run
```
系统将自动加载 `.env`、初始化 `novels.db` 数据库、加载 1.2 万条词库并运行示例场景推导。

---

## 三、 核心 CLI 命令与质量门禁

### 1. 故事线生成示例程序
运行预设的爱恨情仇故事线测试程序：
```powershell
cargo run --bin run_childhood_rivalry
```

### 2. 全量测试套件 (126 项测试)
```powershell
cargo test --all-targets
```

### 3. Rust 代码质量与规范校验
```powershell
# 1. 格式化检查
cargo fmt --all -- --check

# 2. 编译快查
cargo check --all-targets

# 3. 静态代码分析门禁 (Strict Clippy)
cargo clippy --all-targets --all-features -- -D warnings
```

### 4. 词库与正文质量评估报告 (Python 工具)
```powershell
python tools/distill_quality_report.py
```

---

## 四、 N5 长线物理状态机使用指南

在长篇叙事中，可通过 `db.states()` API 管理与监控角色的物理状态，防范逻辑漂移：

### 1. 状态定义 (`CharacterState`)
```rust
use novels::models::CharacterState;

// 1. 角色存活且正常 (默认)
let state_alive = CharacterState::Alive;

// 2. 角色死亡 (Dead) - 将被逻辑断言拦截器强行阻止推导
let state_dead = CharacterState::Dead;

// 3. 角色不在场/离场 (Absent) - 阻止推导
let state_absent = CharacterState::Absent;

// 4. 角色负伤 (Injured)
let state_injured = CharacterState::Injured("左臂受剑伤".into());

// 5. 角色持有物品 (Holding)
let state_holding = CharacterState::Holding("佩剑与信笺".into());
```

### 2. 在代码中设置与查询状态
```rust
let cid = character_id;

// 设置状态
db.states().set_state(cid, &CharacterState::Dead).await?;

// 查询状态
if let Some(state) = db.states().get_state(cid).await? {
    println!("角色当前状态: {:?}", state);
}
```

---

## 五、 提示词工程与确定性生成指南

本引擎实现了提示词与代码的彻底解耦。使用者只需填充结构化参数即可自动构造 Prompt：

### 1. 确定性解码配置
为保证在相同 Prompt 下 DeepSeek V4 Flash 的**语义与输出 100% 确定且无漂移**，请求参数必须设置为：
* `temperature = 0.0` (贪婪采样)
* `top_p = 1.0`
* `seed = 42`

### 2. 使用 `novels::prompt` 自动构建提示词
```rust
use novels::prompt::{build_derivation_prompt, build_system_prompt, DerivationPromptInput};
use std::collections::BTreeMap;

// 1. 获取系统角色提示词 (System Prompt)
let system_prompt = build_system_prompt();

// 2. 构造候选词库 (使用 BTreeMap 确保字典序确定性)
let mut candidates_by_sense = BTreeMap::new();
// ... 插入候选词集 ...

// 3. 填充输入参数
let input = DerivationPromptInput {
    character_name: "姜宁",
    character_tags: &["坚韧".to_string(), "投资人".to_string()],
    character_skills: &["反向收购".to_string()],
    scene_objective_event: "慈善拍卖会上与陆沉重逢",
    memories: &["十年前家道破产".to_string()],
    candidates_by_sense: &candidates_by_sense,
};

// 4. 生成规范的 User Prompt
let user_prompt = build_derivation_prompt(&input);
```

---

## 六、 Web 工作台与 RESTful API 接口说明

系统内置了基于 Axum 0.8 的 HTTP API 服务与 SPA 嵌入式关系图谱工作台。

### 1. 启动 API Web 服务
```powershell
cargo run
```
默认监听地址：`http://127.0.0.1:3000`

### 2. RESTful API 接口概览

| HTTP 方法 | 路径 | 功能说明 | 请求/响应示例 |
|-----------|------|----------|--------------|
| **POST** | `/api/characters` | 创建新角色 | `{"name":"陆沉","personality":["冷峻"],"skills":["资本"]}` |
| **GET** | `/api/characters` | 获取所有角色列表 | 返回 `[{"id":"...","name":"陆沉", ...}]` |
| **POST** | `/api/scenes` | 创建新场景 | `{"objective_event":"拍卖会重逢","participant_ids":["..."]}` |
| **POST** | `/api/scenes/:id/derive` | 执行场景推导与正文生成 | 返回角色感官推导与拼装正文片段 |

---

## 七、 总结

Novels Engine 严格遵循 Rust 高能安全标准与 Superpowers 6 阶段质量门禁，助您打造无 AI 腔、高连贯性、原著质感满满的下一代小说引擎！
